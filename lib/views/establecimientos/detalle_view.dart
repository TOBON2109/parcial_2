import 'package:flutter/material.dart';

class DetalleView extends StatelessWidget {
  const DetalleView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Detalle')));
  }
}
