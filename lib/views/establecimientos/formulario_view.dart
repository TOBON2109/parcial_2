import 'package:flutter/material.dart';

class FormularioView extends StatelessWidget {
  const FormularioView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Formulario')));
  }
}
