import 'package:flutter/material.dart';

class AccidentesView extends StatelessWidget {
  const AccidentesView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Accidentes')));
  }
}
